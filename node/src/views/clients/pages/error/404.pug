extends ../../layout/default.pug

block main
  style.
    .error-404-container {
      min-height: 80vh;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      background: linear-gradient(120deg, #a1c4fd 0%, #c2e9fb 100%);
      padding: 40px 0;
    }
    .error-404-code {
      font-size: 7rem;
      font-weight: 900;
      color: #304ffe;
      text-shadow: 4px 8px 32px rgba(48,79,254,0.15);
      letter-spacing: 8px;
      margin-bottom: 0.5rem;
      line-height: 1;
    }
    .error-404-title {
      font-size: 2.2rem;
      font-weight: 700;
      color: #222;
      margin-bottom: 1rem;
      letter-spacing: 1px;
    }
    .error-404-desc {
      font-size: 1.18rem;
      color: #555;
      margin-bottom: 2.2rem;
      text-align: center;
      max-width: 430px;
    }
    .error-404-btn {
      background: linear-gradient(90deg, #4e54c8 0%, #8f94fb 100%);
      color: #fff;
      font-weight: 600;
      border: none;
      border-radius: 25px;
      padding: 0.75rem 2.2rem;
      font-size: 1.1rem;
      box-shadow: 0 4px 16px rgba(78,84,200,0.12);
      transition: background 0.3s, box-shadow 0.3s, transform 0.2s;
      text-decoration: none;
      outline: none;
      display: inline-flex;
      align-items: center;
      gap: 8px;
    }
    .error-404-btn:hover, .error-404-btn:focus {
      background: linear-gradient(90deg, #8f94fb 0%, #4e54c8 100%);
      color: #fff;
      transform: translateY(-2px) scale(1.04);
      box-shadow: 0 8px 28px rgba(78,84,200,0.18);
      text-decoration: none;
    }
    .error-404-illustration {
      width: 220px;
      max-width: 90vw;
      margin-bottom: 1.5rem;
      display: block;
      filter: drop-shadow(0 6px 24px rgba(48,79,254,0.13));
      user-select: none;
      pointer-events: none;
    }

  .error-404-container
    img.error-404-illustration(src=settingereral.logo alt="404 Illustration")
    div.error-404-code 404
    div.error-404-title Không tìm thấy trang!
    div.error-404-desc
      | Trang bạn đang tìm kiếm không tồn tại, đã bị xóa hoặc chuyển sang địa chỉ khác.
      br
      | Vui lòng kiểm tra lại đường dẫn hoặc quay về trang chủ.
    a.error-404-btn(href="/")
      i.fas.fa-home.me-2
      | Quay về trang chủ
