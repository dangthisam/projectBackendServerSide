extends ../../layout/default.pug
include ../../mixins/alert.pug

block main
  +alertError(5000)
  .container.my-5
    .row.justify-content-center
      .col-md-7.col-lg-5
        .reset-password-card
          h1.text-center.mb-4.fw-bold Đổi mật khẩu
          form(action="/user/password/reset", method="POST", autocomplete="off")
            .mb-3
              label.form-label(for="password")
                i.fas.fa-lock.me-2.text-warning
                | Mật khẩu mới
              input.form-control.shadow-sm(
                type="password"
                name="password"
                id="password"
                required
                minlength="6"
               
                autocomplete="new-password"
              )
            .mb-3
              label.form-label(for="confirm_password")
                i.fas.fa-lock.me-2.text-warning
                | Xác nhận mật khẩu
              input.form-control.shadow-sm(
                type="password"
                name="confirm_password"
                id="confirm_password"
                required
                minlength="6"
                
                autocomplete="new-password"
              )
            button.btn.btn-gradient.btn-lg.w-100.mt-2(type="submit")
              i.fas.fa-sync-alt.me-2
              | Đổi mật khẩu

          //- Thông báo nhỏ về bảo mật
          .mt-3.text-center
            span.text-muted
              i.fas.fa-info-circle.me-1
              | Mật khẩu mới nên khác mật khẩu cũ và đủ mạnh để bảo vệ tài khoản của bạn.
