extends ../../layout/default.pug
include ../../mixins/box-head.pug
include ../../mixins/products-layout.pug

block main
  .container.my-3
    .row
      .col-12
        +box-head(pageTitle)
      .col-12
        if(products)   
            +products-layout(products.length>0)
        else 
             h1 Không có sản phẩm nào 